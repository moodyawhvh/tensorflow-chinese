> 🌐 本文档由 [tensorflow/tensorflow](https://github.com/tensorflow/tensorflow) 翻译,英文原版见原项目。

# 安全地使用 TensorFlow

本文档讨论 TensorFlow 的安全模型,描述在使用模型、检查点或输入数据进行训练或推理服务时需要考虑的安全风险。我们还给出了"什么构成 TensorFlow 漏洞"的判定标准,以及如何报告漏洞。

本文档同样适用于 TensorFlow 组织下的其他仓库,覆盖整个 TensorFlow 生态的安全实践。

## TensorFlow 模型就是程序

TensorFlow
[**模型**](https://developers.google.com/machine-learning/glossary/#model)(借用机器学习从业者常用的说法)被表达为 TensorFlow 所执行的程序。TensorFlow 程序被编码为计算
[**图**](https://developers.google.com/machine-learning/glossary/#graph)。既然模型实际上就是 TensorFlow 执行的程序,使用不受信任的模型或图就等同于运行不受信任的代码。

如果你必须运行不受信任的模型,请在
[**沙箱**](https://developers.google.com/code-sandboxing)内执行。只有当 TensorFlow 算子中的内存破坏问题可以通过生产级的良性模型触达并利用时,才会被认定为安全问题。

### 已保存的图与检查点

加载不受信任的序列化计算图(`GraphDef`、`SavedModel` 或等价的磁盘格式)时,由于 TensorFlow 可用的计算原语足够强大,你应当默认 TensorFlow 进程实际上在执行任意代码。

加载不受信任检查点的风险取决于你所使用的代码或图。加载不受信任的检查点时,模型中被追踪的变量值同样不可信。这意味着如果你的代码会访问文件系统、网络等,并且把检查点变量用在这些交互中(例如用字符串变量拼接文件系统路径),恶意构造的检查点就可能改变这些操作的目标,从而导致任意读/写/执行。

### 运行 TensorFlow 服务端

TensorFlow 是一个分布式计算平台,因此存在 TensorFlow 服务端(`tf.train.Server`)。该服务端仅用于内部通信,并非为不受信任的环境或网络而设计。

出于性能考虑,默认的 TensorFlow 服务端不包含任何授权协议,消息也不加密。它接受来自任何地方的连接,并且不做事先检查就直接执行收到的图。因此,如果你在网络中运行 `tf.train.Server`,任何能访问该网络的人都能以运行 `tf.train.Server` 的用户权限执行任意代码。

## 训练与预测期间的不受信任输入

TensorFlow 支持多种输入数据格式,例如图像、音频、视频和文本。有多个模块专门负责接收这些格式、修改它们,以及/或者把它们转换为 TensorFlow 可处理的中间格式。

这些修改和转换由各种库完成,它们的安全属性各不相同,处理不受信任数据时的可信度也不同。基于这些库的安全历史,我们认为使用不受信任的 PNG、BMP、GIF、WAV、RAW、RAW\_PADDED、CSV 和 PROTO 格式输入是安全的。所有其他输入格式(包括 tensorflow-io)在处理不受信任数据时都应放入沙箱。

例如,如果攻击者上传一个恶意视频文件,就可能利用 TensorFlow 视频处理代码中的漏洞,进而在运行 TensorFlow 的系统上执行任意代码。

请务必让 TensorFlow 保持最新安全补丁,并遵循上述沙箱准则,以防范此类漏洞。

## 执行模式的安全属性

TensorFlow 有多种执行模式,v2 默认使用 Eager 模式。Eager 模式允许用户编写易于检查和调试的命令式语句,主要面向开发阶段。

作为 Eager 模式更易调试的差异之一,[形状推断函数](https://www.tensorflow.org/guide/create_op#define_the_op_interface)会被跳过,形状推断代码内部实现的任何检查都不会执行。

跳过这些检查的安全影响应当很低,因为相应的攻击场景要求恶意用户能够控制模型——如上所述,这本身就已经等同于代码执行。无论如何,我们不建议用 Eager 模式做模型推理服务,因为它同样存在性能限制。

## 多租户环境

可以并行运行多个 TensorFlow 模型。例如,`ModelServer` 会汇集暴露给它的所有计算图(来自多个 `SavedModel`),并在可用执行器上并行运行。以多租户设计运行 TensorFlow,会把上文描述的风险与多租户配置固有的风险叠加在一起。主要关注点是租户隔离、资源分配、模型共享和硬件攻击。

### 租户隔离

由于任何提供模型、图或检查点的租户或用户都能在 TensorFlow 服务的上下文中执行代码,因此必须设计隔离机制,防止对其他租户数据的越权访问。

不同模型之间的网络隔离同样重要,这不仅能防止对数据或模型的未授权访问,也能防止恶意用户或租户以另一个租户的身份提交图来执行。

隔离机制的设计与实现是使用者自身的责任,因此因缺失隔离机制而产生的安全问题不会被认定为 TensorFlow 漏洞。

### 资源分配

某个模型造成的拒绝服务可能拖垮整台服务器,但我们不将其视为漏洞,因为模型有很多种耗尽资源的方式,并且已有成熟的手段可以预防(例如速率限制、ACL、自动重启异常服务的监控等)。

### 模型共享

如果多租户设计允许共享模型,请确保租户和用户了解本文所述的安全风险,并且清楚他们实际上是在运行其他用户提供的代码。目前没有可靠的方法检测恶意模型/图/检查点,因此该场景下推荐的缓解方式是对模型执行做沙箱隔离。

### 硬件攻击

物理 GPU 或 TPU 也可能成为攻击目标。[已发表的研究](https://scholar.google.com/scholar?q=gpu+side+channel)表明,利用 GPU 侧信道攻击有可能泄漏同一系统中其他正在运行的模型或进程的数据。GPU 也可能存在实现缺陷,使攻击者能够驻留恶意代码,或泄漏、篡改其他用户的应用。请向受影响硬件加速器的厂商报告此类漏洞。

## 报告漏洞

### TensorFlow 中的漏洞

本文档介绍了 TensorFlow 的多种使用场景,并注明哪些是推荐或视为安全的用法、哪些场景处理不受信任数据时建议采取某种形式的隔离。因此,本文档也划定了我们认为构成 TensorFlow 安全漏洞的问题范围。

只有发生在我们声明为安全场景中的问题才会被认定为漏洞;仅在 TensorFlow 被以不推荐方式使用时才有安全影响的问题(例如运行不受信任的模型或检查点、解析安全格式列表以外的数据等)不按漏洞处理。

### 报告流程

请使用 [Google Bug Hunters 报告表单](https://g.co/vulnz)报告安全漏洞。报告时请附带以下信息:

  - 一个描述性的标题
  - 你的姓名和所属机构(如有)。
  - 漏洞技术细节的描述。
  - 漏洞的最小复现示例。让我们知道如何复现你的发现非常重要。对于可在 TensorFlow 模型中触发的内存破坏问题,请演示针对 Alphabet 模型(<https://tfhub.dev/>)的利用。
  - 说明谁可以利用该漏洞,以及利用后能获得什么。写出一个攻击场景,展示该问题如何违背威胁模型中定义的使用场景与安全假设。这有助于我们快速评估你的报告,尤其是复杂问题。
  - 该漏洞是否已公开或被第三方知晓。如果是,请提供细节。

我们会尽快修复问题。漏洞修复通常会按季度批量随版本发布。我们会公开致谢发现安全问题的报告者;如你要求,也可以对姓名保密。更多信息请参见 Google Bug Hunters 项目网站。
